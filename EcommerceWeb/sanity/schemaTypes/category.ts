import { title } from "process";

export default{
    name:'category',
    title:'Categories',
    type:'document',
    fields:[
        {
            name:'name',
            title:'name of Category',
            type:'string'
        }
    ]
}